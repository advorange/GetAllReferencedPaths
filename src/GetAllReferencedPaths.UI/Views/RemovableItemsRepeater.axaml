<ItemsRepeater xmlns="https://github.com/avaloniaui"
							 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
							 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
							 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
							 mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
							 x:Class="GetAllReferencedPaths.UI.Views.RemovableItemsRepeater"
							 Name="Me">
	<ItemsRepeater.ItemTemplate>
		<DataTemplate>
			<Grid ColumnDefinitions="auto,*">
				<Button Grid.Column="0" Classes="delete"
								Command="{Binding #Me.RemoveItem}"
								CommandParameter="{Binding}" />

				<ContentControl Grid.Column="1" Name="Content" Content="{Binding}" />
			</Grid>
		</DataTemplate>
	</ItemsRepeater.ItemTemplate>
</ItemsRepeater>